<div class="container">
  <h1>Restaurants</h1>
  <%= link_to "Create new restaurant", new_restaurant_path, class: "btn btn-primary" %><br>
  <% @restaurants.each do |restaurant| %>
  <%= link_to restaurant.name, restaurant_path(restaurant) %><br>
  <% end %>
</div>
